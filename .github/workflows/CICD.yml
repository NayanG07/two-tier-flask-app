name: CICD

on: 
  workflow_dispatch:

jobs:

  code:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        run: echo "Code clone from Github."

  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build the code
        run: echo "Docker container has build"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Testing the container
        run: echo "Testig the container"

  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploing the code
        run: echo "Coad has been deploy"
